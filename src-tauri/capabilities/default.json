{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables default permissions for SQL, Dialog, Opener and Store plugins",
  "windows": [
    "*"
  ],
  "permissions": [
    "core:default",
    "sql:default",
    "sql:allow-execute",
    "sql:allow-select",
    "dialog:default",
    "opener:default",
    "store:default"
  ]
}
